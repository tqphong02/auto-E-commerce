{
  "name": "auto-ecomere",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "auth": "npx playwright test scripts/auth.setup.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "typescript": "^5.1.6",
    "@types/node": "^20.11.0"
  }
}
